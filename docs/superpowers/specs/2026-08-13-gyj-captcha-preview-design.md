# GYJ 验证码预览设计

GYJ 后台登录检测到验证码时，后端从当前登录页中仅截取可见验证码图片，编码为临时 PNG data URL 并返回给当前工具账号。`/inbound` 的 GYJ 验证码输入框旁显示该图片，并提供刷新按钮，刷新只重新读取当前页面的验证码图片。

验证码图片不写入文件、日志、任务或浏览器存储；接口只允许当前工具账号访问其独立 GYJ Worker。若页面没有可识别图片（例如短信验证码），页面明确提示用户从手机/短信获取验证码，仍可手动提交。
